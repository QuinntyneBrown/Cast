<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>Cast.Cli</RootNamespace>
    <AssemblyName>cast</AssemblyName>

    <!-- Packaged as a global/local dotnet tool: `dotnet tool install` then `cast ...` -->
    <PackAsTool>true</PackAsTool>
    <ToolCommandName>cast</ToolCommandName>
    <PackageId>Cast.Cli</PackageId>
    <Version>1.0.0</Version>
    <Description>Scaffolds initial PlantUML sequence diagrams from command-line participants and messages.</Description>

    <!-- Quality gates -->
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
    <EnforceCodeStyleInBuild>true</EnforceCodeStyleInBuild>
    <InvariantGlobalization>true</InvariantGlobalization>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="System.CommandLine" Version="2.0.8" />
    <PackageReference Include="Microsoft.Extensions.DependencyInjection" Version="10.0.8" />
    <PackageReference Include="Microsoft.Extensions.Logging" Version="10.0.8" />
    <PackageReference Include="Microsoft.Extensions.Logging.Console" Version="10.0.8" />
  </ItemGroup>

</Project>
